# Modern Shell Bridge Data Map

Purpose: every modern-shell or legacy-layout port must declare its data source before it ships. If a component cannot point at a real OpenClaw/Mastra bridge endpoint or a deliberately local-only source, it is marked **needs Mastra wiring**.

Checked source files:

- `src/app/utils/runtimeBridge.ts`
- `src/app/hooks/useRuntimeSnapshot.ts`
- `scripts/openclaw-local-server.mjs`
- `src/app/shell/*`
- `src/app/routes/*`
- `src/app/components/{CallFloorPanel,CallModeTab,DocumentPdfPanel,ScriptPanel}.tsx`

## Shared Runtime Feed

Components using `useRuntimeSnapshot()` are fed by this polling bundle:

| Feed             | Endpoint                         | Exists | Notes                                                                                                                                                                  |
| ---------------- | -------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime snapshot | `GET /state` or `GET /api/state` | Yes    | Includes `settings`, `approvals`, `activity`, `leadImports`, `analyzerRuns`, `calls`, `messages`, `contracts`, `adminTasks`, `adminAudit`, and related runtime arrays. |
| Quotas           | `GET /api/quotas`                | Yes    | Used for provider quota cards/status in settings-like surfaces.                                                                                                        |
| Tooling status   | `GET /api/tooling/status`        | Yes    | Used for repo/tooling/provider readiness. Also currently carries some agent/tool summary data used by Agent Fleet and Command Center.                                  |

## Existing Ported Shell Components

| Component / surface                      | Data source                                                                       | Endpoint exists?                                                                                             | Ship status                                                                                                                                                                                                   |
| ---------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shell chrome visual layer                | `ShellTopbar`, `Sidebar`, `FavoritesBar`, `ParadiseLayout`                        | Yes: `GET /state`, `PATCH /api/settings`; local: `localStorage:pbk:favorites:v1`; build env release metadata | Ships. The chrome now exposes source annotations for runtime search/account, autopilot settings writes, local favorites, and build metadata.                                                                  |
| `ParadiseLayout` approval badge plumbing | `useRuntimeSnapshot()` -> `snapshot.approvals`                                    | Yes: `GET /state`                                                                                            | Ships. Badge count is derived client-side via `getPendingApprovalCount`.                                                                                                                                      |
| `Sidebar` nav and Inbox badge            | Prop from `ParadiseLayout`                                                        | Yes indirectly: `GET /state`                                                                                 | Ships. Release label is build env/local build, not bridge-fed. That is acceptable as build metadata, not business data.                                                                                       |
| `ShellTopbar` account identity           | `snapshot.settings.account`, `snapshot.status.account/profile`, fallback env vars | Yes: `GET /state`; write via `PATCH /api/settings`                                                           | Ships. If future auth becomes source of truth, account identity should move to an auth/profile endpoint.                                                                                                      |
| `ShellTopbar` global lead search         | `snapshot.leadImports` plus local `AGENT_REGISTRY`                                | Partial. `GET /state` exists; agent registry is local static.                                                | Ships as basic search. Needs Mastra wiring if search must cover full lead roster, messages, contracts, remote agents, or semantic search. Candidate endpoint: `GET /api/search` exists but is not wired here. |
| `ShellTopbar` Autopilot toggle           | `PATCH /api/settings` with `operatingMode`                                        | Yes                                                                                                          | Ships. This is a real bridge write.                                                                                                                                                                           |
| `FavoritesBar` pinned pages              | `localStorage` only                                                               | Local-only by design                                                                                         | Ships only as single-browser preference. Needs Mastra wiring if favorites must roam across devices/users. Candidate: `PATCH /api/settings` under `settings.ui.favorites`.                                     |
| `ShortcutCheatSheet` / shortcut dispatch | UI-only, window events                                                            | Local-only by design                                                                                         | Ships if shortcuts are treated as local chrome. Needs wiring only if shortcuts should become audited operator actions.                                                                                        |

## Command Center

| Component / section                             | Data source                                                                                              | Endpoint exists?                                                                                  | Ship status                                                                                 |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Header bridge status                            | `useRuntimeSnapshot()` loading/error plus snapshot presence                                              | Yes: `GET /state`                                                                                 | Ships.                                                                                      |
| KPI strip                                       | `snapshot.calls`, `snapshot.leadImports`, `snapshot.approvals`, `snapshot.adminTasks`, `tooling.summary` | Yes: `GET /state`, `GET /api/tooling/status`                                                      | Ships. Counts are client aggregations over bridge state.                                    |
| `LiveCallWidget`                                | `snapshot.calls[0]` mapped into live call state                                                          | Yes: `GET /state`                                                                                 | Ships.                                                                                      |
| Live call Takeover/Mute/End                     | `controlRuntimeCall()`                                                                                   | Yes: `POST /api/calls/:id/action`                                                                 | Ships.                                                                                      |
| Admin task queue                                | `snapshot.adminTasks`                                                                                    | Yes: `GET /state`                                                                                 | Ships.                                                                                      |
| Admin Approve/Decline                           | `updateAdminTaskDecision()`                                                                              | Yes: `PUT /api/admin/tasks/:id`                                                                   | Ships.                                                                                      |
| Web-search cognition probe                      | `fetchWebSearchStatusRequest()`                                                                          | Yes: `GET /api/brain/web-search/status`                                                           | Ships.                                                                                      |
| Tooling/support systems cards                   | `tooling` from `GET /api/tooling/status`                                                                 | Yes                                                                                               | Ships.                                                                                      |
| Activity feed                                   | `snapshot.activity`                                                                                      | Yes: `GET /state`                                                                                 | Ships.                                                                                      |
| Approval queue cards                            | `snapshot.approvals`                                                                                     | Yes: `GET /state`                                                                                 | Ships.                                                                                      |
| Approval decision buttons                       | `updateApprovalDecision()`                                                                               | Yes: `PUT /api/approvals/:id`                                                                     | Ships.                                                                                      |
| Rich approval payload preview in Command Center | Currently only name/type/address in Command Center cards                                                 | Data exists in `snapshot.approvals` (`message`, `body`, `text`, `content`, `payload`, `metadata`) | Needs UI port, not endpoint wiring. Inbox already renders preview via `getApprovalPreview`. |

## Leads

| Component / section          | Data source                                                    | Endpoint exists?                                                                                                                                | Ship status                                                                                                                                                     |
| ---------------------------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Leads cockpit/source rail    | `useRuntimeSnapshot()`, detail/call/contract helpers           | Yes: `GET /state`, `GET /api/leads/:id/full`, `GET /api/leads/:id/last-call`, `PATCH /api/leads/:id`, `POST /invoke`, `POST /api/contract/send` | Ships. Source rail honestly marks `GET /api/leads` as a frontend wiring upgrade for replacing snapshot roster with the full bridge roster.                      |
| Lead list / pipeline rail    | `snapshot.leadImports`                                         | Yes: `GET /state`                                                                                                                               | Ships, but this is not the ideal full roster source. For full production roster, wire this to `GET /api/leads` like Inbox compose does.                         |
| Lead full detail             | `fetchLeadFullRequest()`                                       | Yes: `GET /api/leads/:id/full`                                                                                                                  | Ships.                                                                                                                                                          |
| Last call summary            | `fetchLeadLastCallRequest()`                                   | Yes: `GET /api/leads/:id/last-call`                                                                                                             | Ships.                                                                                                                                                          |
| Save lead edit               | `patchLeadRequest()`                                           | Yes: `PATCH /api/leads/:id`                                                                                                                     | Ships.                                                                                                                                                          |
| Call lead                    | `startLeadCallRequest()` -> `/invoke` tool `telnyx_call`       | Yes: `POST /invoke`; tool exists by bridge contract                                                                                             | Ships. If Mastra becomes the orchestration layer, keep `telnyx_call` registered and audited there.                                                              |
| Send contract                | `sendLeadContractRequest()`                                    | Yes: `POST /api/contract/send`                                                                                                                  | Ships. Approval-gated behavior is returned by bridge status/result.                                                                                             |
| Lead activity section        | Returned by full-detail payload; fallback arrays from snapshot | Yes via `GET /api/leads/:id/full`                                                                                                               | Ships if bridge includes activity. If activity is sparse, needs bridge enrichment, not frontend mock data.                                                      |
| Contract template/path cards | Local `TEMPLATE_NAMES`, `PATH_LABELS` based on lead path       | Local deterministic config                                                                                                                      | Ships. Needs endpoint only if templates become remote/admin editable. Existing candidate endpoints: `GET /api/contracts/templates`, `GET /api/contracts/paths`. |

## Inbox

| Component / section       | Data source                                         | Endpoint exists?                                           | Ship status                                                                                                   |
| ------------------------- | --------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Approval queue            | `snapshot.approvals`                                | Yes: `GET /state`                                          | Ships.                                                                                                        |
| Approval preview/body     | `getApprovalPreview(approval)` from snapshot fields | Yes: `GET /state`                                          | Ships. No hidden mock payload.                                                                                |
| Approval decision         | `updateApprovalDecision()`                          | Yes: `PUT /api/approvals/:id`                              | Ships.                                                                                                        |
| Message list              | `fetchMessagesRequest()`                            | Yes: `GET /api/messages?limit=&offset=`                    | Ships.                                                                                                        |
| Compose lead autocomplete | `fetchLeadsRequest()`                               | Yes: `GET /api/leads`                                      | Ships. This is the better lead roster source.                                                                 |
| Compose send now          | `sendMessageRequest()`                              | Yes: `POST /api/lead/send-message`                         | Ships. Bridge decides SMS/email provider path.                                                                |
| Compose send later        | `scheduleMessageRequest()`                          | Yes: `POST /api/messages`                                  | Ships.                                                                                                        |
| Reply from message row    | Same compose/send path                              | Yes: `POST /api/lead/send-message` or `POST /api/messages` | Ships.                                                                                                        |
| Draft persistence         | `localStorage`                                      | Local-only by design                                       | Ships only as browser-local draft. Needs Mastra/bridge wiring if drafts must roam or recover across browsers. |
| SMS segment count         | Client utility `getSmsSegmentInfo`                  | Local deterministic logic                                  | Ships. No bridge endpoint needed unless billing quote must come from Telnyx.                                  |
| Message archive/swipe     | Not currently persisted                             | No: proposed `PATCH /api/messages/:id/archive`             | Needs Mastra wiring before the prototype swipe/archive action can ship.                                       |

## Agent Fleet

| Component / section          | Data source                                                      | Endpoint exists?                               | Ship status                                                                                                                                                                                     |
| ---------------------------- | ---------------------------------------------------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Base agent roster            | Local `AGENT_REGISTRY`                                           | Local static                                   | Ships for local/catalog agents. Needs Mastra wiring if remote agents should be canonical. Existing candidates: `GET /api/agents/registry`, `GET /api/agents/health`, `POST /api/agents/invoke`. |
| Live agent/tool status merge | `fetchRuntimeToolingStatus()`                                    | Yes: `GET /api/tooling/status`                 | Ships if tooling response includes agent status. Direct agent registry endpoint is not currently the frontend source.                                                                           |
| SNN worker status            | Local `getSnnWorkerStatus()` plus `/invoke` `getSnnWorkerStatus` | Yes: `POST /invoke`; tool route exists         | Ships. Needs stronger UI/endpoint if SNN state should be durable/observable beyond tool call. Existing candidate: `GET /api/agents/snn-status`.                                                 |
| Lead context picker          | `fetchRuntimeState()` -> `leadImports`                           | Yes: `GET /state`                              | Ships, but full roster should use `GET /api/leads` if this picker must search all sellers.                                                                                                      |
| Deal preview                 | `/invoke` tool `previewAgentDealContext`                         | Yes: `POST /invoke`; tool must stay registered | Ships if tool registered. Mastra should own this if preview is remote-agent routed. Existing direct candidate: `POST /api/agents/deal-context/preview`.                                         |
| Skill test                   | `/invoke` tool `pbk_test_skill`                                  | Yes: `POST /invoke`; tool must stay registered | Ships if tool registered.                                                                                                                                                                       |
| Skill transfer               | `/invoke` tool `pbk_transfer_agent_skill`                        | Yes: `POST /invoke`; tool must stay registered | Ships for bridge-confirmed transfer. Needs Mastra wiring if transfer history must be persisted and replayable from a canonical skills table.                                                    |
| Call selected lead           | `startLeadCallRequest()` -> `/invoke` `telnyx_call`              | Yes: `POST /invoke`                            | Ships.                                                                                                                                                                                          |

## Analytics

| Component / section            | Data source                                         | Endpoint exists?                                     | Ship status                                             |
| ------------------------------ | --------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------- |
| Funnel cards and drill-down    | `fetchLeadStagesRequest()`                          | Yes: `GET /api/leads/stages?groupBy=stage&limit=500` | Ships.                                                  |
| Daily deals timeline           | `fetchDealTimelineRequest()`                        | Yes: `GET /api/deals/timeline?days=30`               | Ships.                                                  |
| ROI and AI performance metrics | `fetchAiMetricsRequest()` plus analytics view model | Yes: `GET /api/observability/ai-metrics?days=30`     | Ships if bridge returns spend/revenue/latency fields.   |
| CSV export                     | Client-generated from loaded endpoint data          | No separate endpoint required                        | Ships. Export is not mock; it serializes current model. |

## Memory & Analytics

| Component / section    | Data source                                                               | Endpoint exists?                                          | Ship status                                                                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Skill table            | `fetchSkillOutcomesRequest()`                                             | Yes: `GET /api/skills/outcomes`                           | Ships.                                                                                                                                                                                                                                                  |
| Skill trend sparklines | `fetchSkillTrendsRequest()` per skill                                     | Yes: `GET /api/skills/trends?skillId=&skillName=&days=30` | Ships.                                                                                                                                                                                                                                                  |
| Active experiment card | Derived from `buildMemoryAnalyticsViewModel()` over skill outcomes/trends | Partial                                                   | Ships only if bridge returns experiment-shaped outcome data. If we want formal A/B test management, needs Mastra wiring to a real experiments endpoint/table. Existing generic candidates: `GET /api/emotion/policies/experiments`, but not wired here. |

## Settings

| Component / section        | Data source                                                      | Endpoint exists?                | Ship status                                                                                                                                                                             |
| -------------------------- | ---------------------------------------------------------------- | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime/settings status    | `useRuntimeSnapshot()` -> `snapshot.settings`, `snapshot.status` | Yes: `GET /state`               | Ships.                                                                                                                                                                                  |
| Provider quotas            | `quotas` from `fetchRuntimeQuotas()`                             | Yes: `GET /api/quotas`          | Ships.                                                                                                                                                                                  |
| Tooling/support systems    | `tooling` from `fetchRuntimeToolingStatus()`                     | Yes: `GET /api/tooling/status`  | Ships.                                                                                                                                                                                  |
| Admin task queue           | `snapshot.adminTasks`                                            | Yes: `GET /state`               | Ships.                                                                                                                                                                                  |
| Admin task decision        | `updateAdminTaskDecision()`                                      | Yes: `PUT /api/admin/tasks/:id` | Ships.                                                                                                                                                                                  |
| Metrics/observability link | URL/string from tooling/settings/status payloads                 | Partial                         | Ships as display/link if bridge supplies a URL. Needs bridge/settings wiring only if we want a guaranteed canonical dashboard URL. Existing candidate: `GET /api/observability/status`. |

## Deal / Analyzer Engine

| Component / section                 | Data source                                                | Endpoint exists?                                                          | Ship status                                                                                                           |
| ----------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| Deal analyzer form and calculations | Local React state plus locked `dealCalculations` utilities | Local deterministic engine                                                | Ships. No bridge endpoint needed for pure calculator fields.                                                          |
| Analyze deal bridge sync            | `syncDealAnalysis()` -> `/invoke` tool `analyzeDeal`       | Yes: `POST /invoke`; also direct endpoint exists: `POST /api/analyzeDeal` | Ships if tool stays registered. If Mastra owns tool plans, prefer direct route or Mastra tool execution confirmation. |
| Send deal to agent / CRM            | `sendDealToAgent()` -> `/invoke` tool `updateCRM`          | Yes: `POST /invoke`; direct CRM endpoints also exist                      | Ships if tool stays registered. Needs Mastra tracing if CRM write should be audited end-to-end.                       |
| Seller docs request                 | `sendSellerDocsRequest()`                                  | Yes: `POST /api/send-seller-docs`                                         | Ships.                                                                                                                |
| Script Panel selected script        | `fetchCurrentScriptRequest()`                              | Yes: `POST /api/scripts/current`                                          | Ships. Falls back to local script candidate only if bridge selection fails.                                           |
| Call Mode notes                     | `saveLeadNoteRequest()`                                    | Yes: `POST /api/leads/add-note`                                           | Ships.                                                                                                                |
| Call Mode follow-up                 | `scheduleAppointmentRequest()`                             | Yes: `POST /api/appointments`                                             | Ships.                                                                                                                |
| Call Mode offer email               | `sendOfferEmailRequest()`                                  | Yes: `POST /api/cold-email/send`                                          | Ships.                                                                                                                |
| Call Mode Add to CRM                | `/invoke` tool `updateCRM`                                 | Yes: `POST /invoke`                                                       | Ships if tool registered.                                                                                             |
| Document PDF panel                  | Direct fetch to `buildRuntimeUrl('/api/documents/pdf')`    | Yes: `POST /api/documents/pdf` and `/api/v1/documents/pdf`                | Ships.                                                                                                                |
| `TopBar` in Deal engine             | Props from local deal/analyzer state                       | Local deterministic                                                       | Ships. If the modern shell topbar should reflect global runtime state instead, use `ShellTopbar` sources above.       |

## Campaigns

| Component / section                  | Data source                                                                               | Endpoint exists?                                | Ship status                                                                                                                                                                                |
| ------------------------------------ | ----------------------------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Campaign list/cards                  | `fetchCampaignsRequest()`                                                                 | Yes: `GET /api/campaigns`                       | Ships. Cards use bridge-returned campaign records, metrics, approval state, and schedule data.                                                                                             |
| Campaign lead source picker          | `fetchCampaignLeadSourcesRequest()`                                                       | Yes: `GET /api/campaigns/lead-sources`          | Ships. Source counts come from the bridge's dynamic lead import filters.                                                                                                                   |
| Campaign wizard template suggestions | `fetchReplyTemplatesRequest()`                                                            | Yes: `GET /api/replies/templates`               | Ships as a real reply-template catalog for starter copy. Dedicated campaign-template analytics still needs Mastra wiring if the UI should rank campaign-specific templates by performance. |
| Campaign wizard autosave             | Local draft key `pbk:campaign-wizard:draft:v1`, then `createCampaignRequest()` when saved | Local draft + `POST /api/campaigns`             | Ships. Local autosave is browser-only; the saved campaign itself is bridge-backed.                                                                                                         |
| Launch approval                      | `requestCampaignApprovalRequest(campaignId, body)`                                        | Yes: `POST /api/campaigns/:campaignId/approval` | Ships. Provider execution is approval-gated by the bridge approval queue.                                                                                                                  |
| Pause/resume campaign                | `patchCampaignRequest(campaignId, body)`                                                  | Yes: `PATCH /api/campaigns/:campaignId`         | Ships. Status changes are persisted through the bridge.                                                                                                                                    |
| Cancel campaign                      | `runCampaignActionRequest(campaignId, { action: 'cancel_campaign' })`                     | Yes: `POST /api/campaigns/:campaignId/actions`  | Ships with confirmation.                                                                                                                                                                   |
| Campaign event capture               | `recordCampaignEventRequest(campaignId, body)`                                            | Yes: `POST /api/campaigns/:campaignId/events`   | Frontend helper exists. Use when provider webhooks or manual event logging are ported into the page.                                                                                       |

## Call Floor

| Component / section             | Data source                                         | Endpoint exists?                                       | Ship status                                                                                                                 |
| ------------------------------- | --------------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| Lead search/results             | Props from parent, typically `snapshot.leadImports` | Yes indirectly: `GET /state`                           | Ships for current parent flow. Needs full-roster wiring if parent should pass `GET /api/leads`.                             |
| Active/live calls               | Props from parent, typically `snapshot.calls`       | Yes: `GET /state`                                      | Ships.                                                                                                                      |
| Start call                      | `startLeadCallRequest()` -> `/invoke` `telnyx_call` | Yes: `POST /invoke`                                    | Ships if tool registered.                                                                                                   |
| Schedule callback               | `scheduleAppointmentRequest()`                      | Yes: `POST /api/appointments`                          | Ships.                                                                                                                      |
| Cancel scheduled callback       | `cancelScheduledCallRequest()`                      | Yes: `POST /api/appointments` with `status: cancelled` | Ships.                                                                                                                      |
| Browser notification permission | Browser API/local permission                        | Local-only                                             | Ships as local notification support. Needs service worker/push wiring if notifications must fire while dashboard is closed. |

## Legacy-Layout Components To Restore Without Fake Data

These are the “make it feel premium again” ports. They should not ship until their data source is chosen.

| Proposed restored component                            | Endpoint/data source                                                                                                                                                                                                                                                                                                                        | Status                                                                                                                                |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Founder command queue / “Do Next”                      | Could synthesize from `GET /state`, `GET /api/messages`, `GET /api/appointments`, `GET /api/calls/summary`; no single priority-ranked endpoint today                                                                                                                                                                                        | **Needs Mastra wiring** for ranking, priority, due time, and action target. Proposed: `GET /api/founder/work-queue`.                  |
| Live revenue cockpit                                   | `GET /api/leads/stages`, `GET /api/deals/timeline`, `GET /api/calls/summary`, `GET /api/revenue/engine/status`                                                                                                                                                                                                                              | Mostly exists. Needs view-model endpoint only if we want one canonical payload.                                                       |
| Ava/Rex intelligence stream                            | `snapshot.activity`, `/events`, `GET /api/agent-decisions`, `GET /api/rex/decisions`                                                                                                                                                                                                                                                        | Partial. **Needs Mastra wiring** if this is a ranked, agent-authored “intelligence stream” instead of raw activity.                   |
| Agent health rail                                      | `GET /api/agents/health`, `GET /api/agents/registry`, `GET /api/tooling/status`                                                                                                                                                                                                                                                             | Endpoints exist. Frontend currently uses local registry plus tooling status. Needs port wiring to direct agent endpoints.             |
| System confidence/source labels                        | Many endpoint responses expose `source` or status flags, but no universal source envelope                                                                                                                                                                                                                                                   | Partial. **Needs Mastra wiring** if every tile must display `source`, `stalenessMs`, `lastUpdatedAt`, and `fallbackReason`.           |
| Unified search across leads/messages/contracts/agents  | `GET /api/search` exists; current shell only searches snapshot leads + local agents                                                                                                                                                                                                                                                         | Endpoint exists but not wired to shell. Needs frontend port wiring and possibly Mastra-backed indexing for semantic search.           |
| Cross-device user prefs                                | Current favorites/theme/rail use localStorage; account from bridge settings                                                                                                                                                                                                                                                                 | Partial. **Needs Mastra wiring** or `/api/settings` schema for per-user prefs.                                                        |
| Production deploy/release panel                        | `GET /api/tooling/status`, `GET /api/observability/status`, Render/Netlify provider status if exposed                                                                                                                                                                                                                                       | Partial. **Needs Mastra wiring** for canonical Render deploy ID, Netlify deploy ID, commit hash, migration status, and env readiness. |
| Campaign analytics drilldown / premium campaign panels | Core route now ships from `GET/POST/PATCH /api/campaigns`, `GET /api/campaigns/lead-sources`, `POST /api/campaigns/:campaignId/approval`, `POST /api/campaigns/:campaignId/actions`, `POST /api/campaigns/:campaignId/events`. More premium panels may need `GET /api/analytics/campaign-drilldown` or a dedicated Mastra summary endpoint. | Partial. Route ships; advanced analytics/source ranking still may need Mastra wiring.                                                 |

## Porting Rule

Before porting a legacy visual component into the modern shell:

1. Name the exact endpoint or local deterministic source.
2. Confirm the endpoint handler exists in `scripts/openclaw-local-server.mjs`.
3. Confirm the frontend helper exists in `src/app/utils/runtimeBridge.ts` or add one.
4. If the endpoint/tool is `/invoke`, list the required `toolName` and treat tool registration as part of the contract.
5. If no endpoint exists, label the component **needs Mastra wiring** and do not ship a mock payload.
6. Empty states must describe the real state: “no approvals waiting,” “bridge returned no rows,” or “Mastra wiring needed.”
