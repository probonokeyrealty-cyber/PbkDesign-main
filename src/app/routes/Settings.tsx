/** Settings — placeholder route for org / connector / branding settings. */
export function Settings() {
  return (
    <div className="p-6 space-y-4">
      <div>
        <h1 className="text-xl font-semibold text-slate-100">Settings</h1>
        <p className="text-sm text-slate-400">
          Branding · MAO underwriting rules · connectors
        </p>
      </div>
      <div className="rounded-lg border border-slate-800 bg-slate-950 p-6 text-center text-xs text-slate-500">
        Placeholder · will host UnderwritingControls + brand fields.
      </div>
    </div>
  );
}
